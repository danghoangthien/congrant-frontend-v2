{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "husky": "^3.1.0",
    "lint-staged": "^12.4.1",
    "prettier": "2.6.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{css,scss,ts,tsx,js,jsx}": [
      "prettier --write"
    ]
  },
  "dependencies": {}
}
